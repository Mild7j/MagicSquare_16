# 13 — MagicSquare 코드 리뷰 작업 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 문서 유형 | code-reviewer 서브에이전트 기반 전체 코드 리뷰 |
| TDD Phase | **GREEN + Refactoring 준비** (Golden Master 구축 직후) |
| 대상 범위 | `src/` ECB 3계층 · Golden Master · 테스트 · 스크립트 |
| 기준 문서 | `.cursor/rules/magicsquare-ecb-architecture.mdc`, `docs/PRD_MagicSquare.md`, Report/12 |
| 작성 기준일 | 2026-05-29 |
| Python | 3.13 |
| 브랜치 | `refactoring` → `magic16/refactoring` |
| 대화 Export | `Prompt/13.MagicSquare_Code_Review_Work_Report.prompt` |

---

## 1. 문서 목적

본 보고서는 `/code-reviewer` 서브에이전트를 이용해 `c:\DEV\MagicSquare_xx` 전체를 리뷰한 결과를 기록한다.  
Golden Master(GM-1~3) 구축 및 `MagicSquare_16` 원격 push 직후 시점의 **품질·아키텍처·회귀 리스크**를 정리한다.

**종합 평가:** **needs work**  
도메인·Golden Master 인프라는 양호하나, ECB 위반·FR-01 미완(duplicate)·Control 이중 경로가 핵심 개선 과제이다.

---

## 2. 리뷰 범위

| 영역 | 검토 대상 |
|------|-----------|
| ECB 경계 | `boundary` → `control` → `entity` 의존 방향 |
| Golden Master | `tests/golden_master/`, `test_golden_master_magic_square.py`, CLI |
| Boundary/Control/Entity | `ui_boundary`, `solve_partial_magic_square`, domain services |
| 테스트 | RED/GREEN 혼재, 마커, 중복 |
| 도구 | `scripts/generate_golden_master.py` |
| 워크스페이스 규칙 | 타입힌트, bare except, 상수 모듈화 |

---

## 3. 강점 (Strengths)

| # | 내용 |
|---|------|
| S-1 | TDD RED 스켈레톤과 GREEN 구현이 레이어별로 분리 |
| S-2 | Golden Master: API 직렬화, approve 패턴, unified diff, `--check` |
| S-3 | `contracts.py`: int[6]·1-index·row-major·Attempt 1/2 + domain oracle |
| S-4 | `src/` 대부분 타입힌트·Google docstring 준수 |
| S-5 | bare `except` 없음; domain 예외 → `ResolveError` 매핑 |
| S-6 | `golden_master` pytest 마커 `pyproject.toml` 등록 |

---

## 4. 이슈 요약

### 4.1 Critical

| ID | 이슈 | 위치 |
|----|------|------|
| **C-1** | GM-TC-04 duplicate 입력 시 `ValueError`가 Boundary 밖으로 누출 | `ui_boundary.py`, `two_cell_solver.py`, `missing_number_finder.py` |

**원인:** `find_not_exist_nums()`가 중복을 검증하지 않음 → `smaller, larger = ...` 언패킹 `ValueError`.  
`UIBoundary.solve()`는 `ResolveError`만 catch. Golden Master baseline은 `Error: ValueError`로 고정.

**권장:** Boundary duplicate 검증(U-IN-07) → `DUPLICATE_NON_ZERO` `ErrorResponse`.

### 4.2 Major

| ID | 이슈 | 위치 |
|----|------|------|
| **M-1** | Control → Boundary 역의존 | `src/control/magic_square_service.py` |
| **M-2** | Boundary → Entity 직접 import | `src/boundary/screen/app.py` |
| **M-3** | Control 이중 오케스트레이션 | `MagicSquareService` vs `SolvePartialMagicSquare` |
| **M-4** | GM-TC-04 baseline이 결함 동작 영구화 | `tests/golden_master_expected.txt` |
| **M-5** | pytest unknown marker (`boundary`/`control`/`entity`) | `pyproject.toml`, DEF-004 |

### 4.3 Minor / Suggestion

| ID | 요약 |
|----|------|
| m-1 | 상수 SSOT 중복 (`BLANK_VALUE`, blank count, `UNSOLVABLE_CODE`) |
| m-2 | `ui_boundary.py` `assert matrix is not None` (`-O`에서 제거) |
| m-3 | `capture_solve_result()` `except BaseException` → `Exception` 권장 |
| m-4 | pytest baseline auto-create가 CI drift 숨길 수 있음 |
| m-5 | 테스트 중복·레이어 교차 import (`test_ui_boundary_solve` → entity conftest) |
| m-6 | `user.py` — Magic Square 도메인과 무관 |
| m-7 | README “구현 이전” 문구와 실제 상태 불일치 |

---

## 5. 파일별 핵심 소견

| 파일 | 판정 | 비고 |
|------|:----:|------|
| `src/boundary/ui_boundary.py` | △ | ECB 준수, `ResolveError` 매핑 ✓ / 기타 예외 미처리 ✗ |
| `src/control/solve_partial_magic_square.py` | ○ | Entity 오케스트레이션·예외 변환 명확 |
| `src/control/magic_square_service.py` | ✗ | Control→Boundary 역의존, stub resolver |
| `src/boundary/screen/app.py` | △ | composition root ✓ / Entity VO 직접 import ✗ |
| `src/entity/services/two_cell_solver.py` | ○ | Attempt 1→2 fallback PRD 일치 |
| `tests/golden_master/*` | ○ | approve·계약·시나리오 SSOT 양호 |
| `scripts/generate_golden_master.py` | ○ | `--check`/generate 분리, CLI `print` 허용 |

---

## 6. Golden Master 리뷰

### 6.1 시나리오 상태

| GM-TC | 상태 | 비고 |
|-------|:----:|------|
| GM-TC-01 | GREEN | Attempt 1 small-first |
| GM-TC-02 | GREEN | reverse fallback |
| GM-TC-03 | GREEN | INVALID_BLANK_COUNT |
| GM-TC-04 | **GAP** | `ValueError` (duplicate 미구현) |
| GM-TC-05 | GREEN | UNSOLVABLE |

### 6.2 Golden Master 리스크

| ID | 리스크 | 심각도 |
|----|--------|--------|
| GM-R1 | GM-TC-04 `ValueError` baseline | Major |
| GM-R2 | baseline 없을 때 pytest auto-create PASS | Minor |
| GM-R3 | per-scenario approve 시 전체 파일 rewrite | Minor |

---

## 7. 권장 후속 작업 (우선순위)

| 우선순위 | 작업 | 근거 |
|----------|------|------|
| **P0** | Boundary duplicate 검증 (U-IN-07) | C-1 ValueError 누출 |
| **P0** | `UIBoundary.solve()` 미처리 예외 방어 | API 안정성 |
| **P1** | `MagicSquareService` / `SolvePartialMagicSquare` 통합 | M-3 SSOT |
| **P1** | ECB 역의존·Entity import 제거 | M-1, M-2 |
| **P2** | GM-TC-04 approve → `DUPLICATE_NON_ZERO` | M-4 |
| **P2** | pytest 마커 등록 (`boundary`, `control`, `entity`) | M-5 |
| **P3** | CI에 `generate_golden_master.py --check` 추가 | 회귀 gate |

---

## 8. 세션 맥락 (선행 작업)

| 일시 | 작업 | 산출물 |
|------|------|--------|
| 동일 세션 | GM-1~3 구축 | Report/12, Golden Master 전체 |
| 동일 세션 | `magic16` push | `refactoring` → `github.com/Mild7j/MagicSquare_16.git` |
| 본 세션 | code-reviewer 리뷰 | 본 Report/13 |

---

## 9. 결론

MagicSquare_xx는 **도메인 solver와 Golden Master 회귀 안전장치가 실용 수준**에 도달했다.  
다음 스프린트는 **FR-01 duplicate 완성(P0)** 과 **ECB 정합성 정리(P1)** 가 핵심이며, duplicate 구현 후 GM-TC-04 baseline approve 갱신으로 Golden Master 계약을 정상화해야 한다.

---

## 10. 관련 문서

- `Report/12.MagicSquare_Golden_Master_Regression_Work_Report.md`
- `docs/golden_master_approve_design.md`
- `docs/defect_list.md` (DEF-004 marker 경고)
