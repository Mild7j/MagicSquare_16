# 14 — MagicSquare ECB 리팩토링 계획 작업 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 문서 유형 | REFACTOR phase 준비 — 테스트 갭 · 코드 스멜 · ECB 분리 · SRP · 리팩토링 계획 |
| TDD Phase | **REFACTOR 준비** (전 RED→GREEN gate 선행 필요) |
| 대상 범위 | `solve_partial_magic_square.py` · `ui_boundary.py` · `screen/app.py` (main_window 대응) |
| 기준 문서 | `.cursorrules` (architecture · dependency_direction · refactor_phase · must_not), Report/13 |
| 작성 기준일 | 2026-05-29 |
| Python | 3.13 |
| 대화 Export | `Prompt/14.MagicSquare_ECB_Refactoring_Plan_Work_Report.prompt` |

---

## 1. 문서 목적

본 보고서는 Report/13 코드 리뷰 이후, **REFACTOR phase**에 들어가기 전 수행한 **사전 분석·계획**을 기록한다.

- 대상 3파일의 **테스트 갭** 및 RED 스켈레톤 GREEN 선행 목록
- **High/Medium/Low** 코드 스멜 및 **ECB 역할·분리** 판정
- **SRP** 위반(함수 다중 역할 · 클래스 복수 책임 · UI 비즈니스 판단)
- **우선순위별 리팩토링 계획** 및 회귀 검증 방법

**코드 변경은 본 세션에서 수행하지 않았다.** 산출물은 분석·계획 문서만이다.

---

## 2. ECB 파일 매핑

| 프롬프트 명칭 | 실제 경로 | ECB 레이어 |
|---------------|-----------|------------|
| `domain.py` | `src/control/solve_partial_magic_square.py` | **Control** |
| `boundary.py` | `src/boundary/ui_boundary.py` | **Boundary** |
| `gui/main_window` | `src/boundary/screen/app.py` *(main_window.py 미존재)* | **Screen** (Boundary 하위 UI) |

**연관 참고 (직접 수정 대상 아님):** `input_validator.py`, `two_cell_solver.py`, `magic_square_service.py`

---

## 3. 테스트 갭 요약 (REFACTOR gate)

`.cursorrules` `refactor_phase`: **모든 테스트 GREEN 후** 리팩터링 시작.

| 대상 | 대응 test_*.py | 상태 |
|------|----------------|------|
| `solve_partial_magic_square.py` | `tests/control/test_solve_partial_magic_square.py` | **없음 (신규 필요)** |
| `ui_boundary.py` | `test_ui_boundary_solve.py` + RED 3파일 | **부분 GREEN** (2건) + RED 12건 |
| `screen/app.py` | `tests/boundary/screen/test_app.py` | **없음 (신규 필요)** |

### RED 스켈레톤 (`pytest.fail`) — 리팩터링 전 GREEN 필수

| 파일 | ID | 건수 | 대상 연관 |
|------|-----|------|-----------|
| `test_u_flow_red.py` | U-FLOW-02a~d | 4 | `ui_boundary` — Control 격리 |
| `test_u_out_red.py` | U-OUT-01~03 | 3 | `ui_boundary` — int[6] 계약 |
| `test_u_in_red.py` | U-IN-04~08 | 5 | `validator` / FR-01 E002/E004/E005 |
| `test_d_sol_red.py` | D-SOL-02~04 | 3 | `solve_partial` → `solution()` 경로 |
| `test_d_mis_red.py` | D-MIS-01 | 1 | `find_not_exist_nums` |
| `test_d_val_red.py` | D-VAL-01~06 | 6 | entity (suite gate) |

**현재 RED 합계: 25건**

### 테스트 없이 REFACTOR 금지 (한 줄)

`.cursorrules`: **“모든 테스트가 통과하는 상태에서만 리팩터링을 시작한다”** — RED·무테스트 상태에서는 동작 불변을 검증할 회귀 게이트가 없다.

---

## 4. ECB 역할 현황

| 파일 | 현재 역할 | 적합 여부 | 근거 |
|------|-----------|-----------|------|
| `solve_partial_magic_square.py` | Control (orchestration + 예외 변환) | **부분** | locate→find→solve 위임 적절; 29–30행 **entity 이중 호출** · blank 예외 Boundary와 중복 |
| `ui_boundary.py` | Boundary (부분 검증 + Control 위임 + envelope) | **부분** | `boundary→control` OK; **E004/E005 미검증** · `ResolveError` 외 예외 누출 (GM-TC-04) |
| `app.py` | Screen + composition root + CLI | **부적합** | UIBoundary 호출 적절; **Control/Entity 직접 import** · bootstrap 혼재 |

### 추가 판정

| 질문 | 답 |
|------|-----|
| `solve_partial` → Entity vs Control? | **Control** — 알고리즘·int[6]·Step A/B는 `two_cell_solver`(Entity) |
| `ui_boundary` must_not 위반? | entity 직접 호출·레이어 이동 **없음**; Boundary must 역할(E001~E005) **미완** |
| Screen UI 외 책임 혼재? | **예** — composition root, Entity VO, G1 fixture, CLI |

---

## 5. 코드 스멜 (High 우선)

| 파일 | 줄 | 스멜 | 우선순위 |
|------|-----|------|----------|
| `ui_boundary.py` | 47–50 | `ResolveError`만 catch → E005 `ValueError` 누출 | **High** |
| `ui_boundary.py` | 37–45 | E004/E005 검증 누락 | **High** |
| `solve_partial_magic_square.py` | 29–31 | entity 호출 이중 + `solution()` 내부 재실행 | **High** |
| `app.py` | 97–99 | int[6] blind unpack | **High** |
| `app.py` | 22–23 | Screen→Entity VO import | **High** |
| `ui_boundary.py` + `magic_square_service.py` | — | 이중 orchestration | **High** |
| `input_validator.py` vs `BoundaryValidator` | — | FR-01 이중 SSOT | **High** |
| `two_cell_solver.py` | 50 | duplicate → `ValueError` (Boundary가 막아야 함) | **High** |

---

## 6. SRP 위반

### ① 함수 다중 역할

| 파일:줄 | 역할 1 | 역할 2 |
|---------|--------|--------|
| `ui_boundary.py:28-50` | FR-01 검증 오케스트레이션 | Control 위임 + `ErrorResponse` 직렬화 |
| `solve_partial_magic_square.py:28-41` | locate→find→solve 오케스트레이션 | Domain→`ResolveError` 변환 |
| `app.py:45-75` | PyQt UI 구성 | `GRID_G1` fixture 데이터 주입 |
| `app.py:106-126` | UI 레이아웃 | `solve(None)` 계약 진단 |
| `app.py:138-156` | QApplication 부트스트랩 | composition + 창 라우팅 |

### ② 클래스 복수 책임

| 파일:줄 | 문제 |
|---------|------|
| `ui_boundary.py:11-50` | DI + 검증 + 위임 + envelope 직렬화 |
| `solve_partial_magic_square.py:13-41` | orchestration + 예외 매핑 |
| `app.py:42-100` | UI 상태 + solve 트리거 + fixture 주입 |
| `app.py:103-126` | UI + `grid=None` 진단 |

**데이터 보관 + 검증·도메인 판단 동시 담당:** 해당 없음 (위임 구조).

### ③ UI 비즈니스 판단

**해당 없음** — magic sum(34), solvability, Step A/B, E00x 코드 결정 없음. DTO→문자열 표현만 수행.

---

## 7. 잘못된 위치 코드 — 이동·분리 대상

| 현재 | 문제 | 이동·분리 대상 |
|------|------|----------------|
| `app.py:21,129-135` | Screen→Control | `boundary/composition.py` 또는 `screen/bootstrap.py` |
| `app.py:22-23` | Screen→Entity VO | `boundary/constants` / UI 상수 |
| `app.py:138-162` | CLI·bootstrap 혼재 | `screen/__main__.py` 또는 `bootstrap.py` |
| `app.py:103-126` | 진단 창 in UI 모듈 | `verify_window.py` 또는 test support |
| `solve_partial:29-30` | entity 이중 호출 | 제거 — `solution()` 단일 위임 |
| `input_validator.py` | NotImplemented vs `BoundaryValidator` | `validator.py` SSOT 통합 |

---

## 8. Control vs Entity 경계

| 책임 | 위치 | 판정 |
|------|------|------|
| Step A/B · int[6] 조립 | `two_cell_solver.solution()` | Entity (적절) |
| locate→find→solve 순서 | `resolve()` 28–31 | Control (적절) |
| 29–30 선행 호출 | `resolve()` | **제거 대상** — `solution()`과 중복 |
| duplicate 입력 | Boundary 미검증 → `ValueError` | **경계 누수** — P0 GREEN 선행 |

---

## 9. 리팩토링 대상 목록 (우선순위 순)

| 순번 | 대상 | 문제 | 적용 기법 | 우선순위 |
|------|------|------|-----------|----------|
| 1 | `ui_boundary` + `validator` | FR-01 미완, 예외 누출, 3역할 혼재 | Extract Method, SSOT 통합 | **P0** |
| 2 | `input_validator.py` | 이중 SSOT | Consolidate | **P0** |
| 3 | U-IN/U-FLOW/U-OUT RED | gate 미충족 | GREEN 전환 | **P0** |
| 4 | GM-TC-04 baseline | `ValueError` 고정 | Approve → E005 | **P0** |
| 5 | `solve_partial` 29–30 | entity 이중 orchestration | Remove Duplication | **P0** |
| 6 | `tests/control/test_solve_partial_*` | Control 무테스트 | 테스트 신규 | **P0** |
| 7 | `app.py` | import·bootstrap 혼재 | Extract Module, DI | **P1** |
| 8 | `tests/boundary/screen/test_app.py` | Screen 무테스트 | 테스트 신규 | **P1** |
| 9 | `magic_square_service.py` | 이중 경로·역의존 | Collapse / SSOT | **P1** |
| 10 | boundary/control constants | code 이중 정의 | Move Constant | **P1** |
| 11 | `grids.py` | fixture 중복 | Move Data | **P2** |
| 12 | `ui_boundary:41` assert | `-O` 제거 | Replace Assert | **P2** |
| 13 | `VerifyNoneWindow` | UI+진단 혼재 | Extract Class | **P2** |

---

## 10. 테스트 선행 필요 함수 목록

### Boundary GREEN

- `UIBoundary.solve` — U-FLOW-02, U-OUT-01~03
- `BoundaryValidator` (FR-01 통합) — U-IN-04~08
- GM-TC-04 duplicate envelope

### Control 신규

- `SolvePartialMagicSquare.resolve` — G1, `ResolveError` 매핑, 호출 순서

### Screen 신규

- `MagicSquareMainWindow._read_grid`, `_on_solve_clicked`, `__init__`
- `build_ui_boundary`, `VerifyNoneWindow.__init__`

### Entity RED (suite gate)

- `solution()` D-SOL-02~04, `find_not_exist_nums` D-MIS-01, `is_magic_square` D-VAL-01~06

---

## 11. 리팩토링 후 검증

### 회귀 테스트 명령

```powershell
pytest tests/ -v
pytest tests/boundary/ tests/control/ -v
pytest tests/boundary/test_u_in_red.py tests/boundary/test_u_flow_red.py tests/boundary/test_u_out_red.py tests/boundary/test_ui_boundary_solve.py -v
pytest tests/control/test_solve_partial_magic_square.py -v
pytest tests/boundary/test_golden_master_magic_square.py -v -m golden_master
python scripts/generate_golden_master.py --check
pytest tests/ --cov=src --cov-report=term-missing --cov-fail-under=80
```

### 외부 동작 불변 확인

| 항목 | 방법 | 기대 |
|------|------|------|
| E001~E005 | U-IN + GM-TC-03/04 | structured `ErrorResponse`; GM-TC-04만 E005 approve |
| E006/E007 | GM-TC-05, resolve tests | `UNSOLVABLE` 등, raw exception 없음 |
| int[6] | U-OUT, GM-TC-01/02, G1 | len=6, 1-index, G1=`[2,2,10,3,3,7]` |
| Control 격리 | U-FLOW-02 | invalid → `resolve()` 0회 |
| ECB import | grep | Screen→UIBoundary만; 역방향 없음 |
| UI smoke | `python -m src.boundary.screen.app` | G1 풀기 동일 결과 |

---

## 12. ECB 분리 실행 순서

```
P0 GREEN (validator·U-IN/U-FLOW/U-OUT·control 단위·GM-TC-04)
  → P0 REFACTOR (ui_boundary envelope · solve_partial 29–30 제거)
  → P1 GREEN (screen tests)
  → P1 REFACTOR (app.py 분리 · Control SSOT)
  → P2 (상수 · fixture · assert)
```

---

## 13. 결론

Report/13 **needs work** 항목(C-1 duplicate, M-1~M-3 ECB, 이중 orchestration)에 대해, **REFACTOR phase 진입 전** 필요한 테스트·스멜·ECB·SRP 분석과 **우선순위 계획**을 확정했다.

**다음 권장 작업:** P0 GREEN — `BoundaryValidator` FR-01 완성(U-IN-05~07) + U-FLOW-02 + control 단위 테스트 신규 → 이후 P0 REFACTOR 착수.

---

## 14. 관련 문서

| 문서 | 내용 |
|------|------|
| Report/13 | 코드 리뷰 (C-1, M-1~M-5) |
| Report/12 | Golden Master GM-1~3 |
| `.cursorrules` | ECB · TDD · refactor_phase |
| `docs/PRD_MagicSquare.md` | FR-01~05, TD-01~06 |
