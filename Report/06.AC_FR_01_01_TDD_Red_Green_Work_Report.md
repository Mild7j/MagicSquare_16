# 06 — AC-FR-01-01 TDD Red-Green 작업 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 문서 유형 | Test Plan · RED Test · Green 구현 · 결함 분석 · QA 검증 |
| 대상 AC | **AC-FR-01-01** (4×4 아닌 입력 → `INVALID_SIZE`), **AC-FR-01-05** (Domain resolver 미호출) |
| 기준 문서 | `docs/PRD_MagicSquare.md`, `docs/test_plan.md` |
| 작성 기준일 | 2026-05-29 |
| Python | 3.13 |
| 대화 Export | `Prompt/06.AC_FR_01_01_TDD_Red_Green_Work_Report.prompt` |

---

## 1. 문서 목적

본 보고서는 Magic Square 4×4 프로젝트에서 **FR-01 Input Verification (Boundary)** 의 첫 Acceptance Criteria인 **AC-FR-01-01**을 대상으로 수행한 Dual-Track TDD 작업(테스트 계획 → RED 테스트 → Green 최소 구현 → 결함 문서화)을 기록한다.

---

## 2. 세션 산출물 요약

| 단계 | 작업 | 산출물 |
|------|------|--------|
| 1 | 테스트 플랜 샘플 예제 선정 | 대화 산출 (AC-FR01-01, `grid=None`) |
| 2 | 테스트 계획서 작성 | `docs/test_plan.md` (루트 `test_plan.md`에서 이동) |
| 3 | README RED To-Do 체크리스트 | `README.md` § RED 단계 To-Do 리스트 |
| 4 | RED 단위·격리 테스트 작성 | `tests/boundary/`, `tests/control/` (27 tests) |
| 5 | Python 3.13 호환 정리 | `tests/boundary/constants.py`, `tests/__init__.py` |
| 6 | Green 최소 구현 | `src/boundary/`, `src/control/`, `src/entity/completion_resolver.py` |
| 7 | 결함 목록 문서화 | `defect_list.md` |
| 8 | QA 결함 분석·회귀 확인 | 본 보고서 §6, §7 |
| 9 | Report / Transcript Export | 본 문서, `Prompt/06.AC_FR_01_01_TDD_Red_Green_Work_Report.prompt` |

---

## 3. 테스트 플랜 샘플 예제 (선정 결과)

| 항목 | 내용 |
|------|------|
| **선택 AC** | AC-FR01-01 |
| **PRD** | FR-01, BR-01, §13, TC-E-01 |
| **입력** | `grid = None` |
| **기대 출력** | `code: "INVALID_SIZE"`, `message: "Grid must be 4x4."` |
| **선정 이유** | FR-01 AC 중 크기 검증 최선행 조건; Boundary 계약 + Domain 격리(AC-FR01-05) 동시 검증 가능 |

---

## 4. RED 단계

### 4.1 테스트 범위

| Track | 디렉터리 | 테스트 수 | 검증 포인트 |
|-------|----------|-----------|-------------|
| **A — Boundary** | `tests/boundary/test_ac_fr_01_01_invalid_size.py` | 18 | INVALID_SIZE 반환, 메시지 문자 단위 일치, 범위 제한 |
| **B — Control** | `tests/control/test_ac_fr_01_01_resolve_isolation.py` | 9 | `resolve()` 0회 호출 (mock/spy) |

### 4.2 경계값 케이스 (AC-FR-01-01 In-Scope)

| 입력 | 기대 |
|------|------|
| `grid = None` | `INVALID_SIZE` |
| `grid = []` | `INVALID_SIZE` |
| `grid = [[]] * 4` | `INVALID_SIZE` |
| `grid` 3×4 | `INVALID_SIZE` |

**제외:** 4×4 정상 입력, AC-FR-01-02~05, FR-02~05

### 4.3 RED 초기 실패 유형

| 유형 | 메시지 | 원인 |
|------|--------|------|
| ImportError | `No module named 'src.boundary'` | 구현 전 conftest import 실패 |
| ImportError | `No module named 'pydantic'` | venv 의존성 미설치 |

---

## 5. GREEN 단계 (최소 구현)

### 5.1 추가·수정 모듈

| 모듈 | 책임 |
|------|------|
| `src/boundary/constants.py` | `GRID_DIMENSION`, `INVALID_SIZE_*` |
| `src/boundary/schemas.py` | pydantic `ErrorResponse` |
| `src/boundary/validator.py` | `BoundaryValidator.validate_size()` |
| `src/control/magic_square_service.py` | 검증 실패 시 early return, `resolve()` 미호출 |
| `src/entity/completion_resolver.py` | Domain 스텁 (`NotImplementedError`) |

### 5.2 ECB 의존 방향 준수

- `control` → `boundary`, `entity` (validator + resolver 주입)
- `boundary` → `entity` 직접 호출 없음
- invalid size 시 Domain `resolve()` 진입 금지 (AC-FR-01-05)

### 5.3 의도적 미구현 (범위 제한)

- AC-FR-01-02 ~ AC-FR-01-04 (빈칸·범위·중복)
- FR-02 ~ FR-05 Domain 로직
- `CompletionResolver.resolve()` 본문

---

## 6. 결함 목록 요약

상세: [`defect_list.md`](../defect_list.md)

| ID | Severity | 상태 | 요약 |
|----|----------|------|------|
| DEF-001 | Critical | Closed | `src.boundary` 미구현 → ImportError |
| DEF-002 | Major | Closed | `pydantic` 미설치 |
| DEF-003 | Info | Closed | Git Bash `py` 런처 없음 → `python -m venv` |
| DEF-004 | Minor | **Open** | `pytest.mark.boundary/control` 미등록 경고 |
| DEF-005 | Info | Closed (예방) | `grid=None` 시 `None` 반환 가능성 → validator 분기 처리 |

---

## 7. 검증 결과

### 7.1 테스트 실행

| 명령 | 결과 |
|------|------|
| `pytest tests/boundary tests/control -v` | **27 passed**, 2 warnings (DEF-004) |
| `pytest tests/ -v` | **34 passed**, 2 warnings (DEF-004) |

### 7.2 로컬 환경 (Git Bash + venv)

```bash
cd /c/DEV/MagicSquare_xx
python -m venv .venv
source .venv/Scripts/activate
python -m pip install pydantic pytest pytest-cov
python -m pytest tests/boundary tests/control -v
```

> Windows Git Bash에서는 `py -3.13` 대신 `python` 사용.

### 7.3 커버리지

- `htmlcov/` 생성 가능 (`pytest --cov=src --cov-report=html`)
- AC-FR-01-01 범위: Boundary/Control early-return 경로 커버

---

## 8. README RED To-Do 연동

`README.md` § RED 단계 To-Do 리스트:

- **결함 목록 연결:** `defect_list.md` 생성·기록 완료, 회귀 34 passed 체크 완료
- **Track A/B 개별 TC:** Green 통과 후 수동 체크 권장 (본 세션에서 자동 일괄 체크는 미수행)

---

## 9. 주요 의사결정

| 결정 | 내용 |
|------|------|
| 오류 code 표기 | 테스트 계약 `INVALID_SIZE` (PRD `ERR_INVALID_SIZE`와 매핑 문서화) |
| `validate_size` 반환 | invalid → `ErrorResponse`, valid 4×4 → `None` (solve에서 resolver 호출) |
| 상수 | `GRID_DIMENSION = 4` 명명 (magic number 금지) |
| 테스트 상수 분리 | `tests/boundary/constants.py` (conftest 직접 import 회피) |

---

## 10. 후속 권장 작업

1. **DEF-004 해소:** `pytest.ini`에 `boundary`, `control` 마커 등록
2. **requirements-dev.txt:** `pytest`, `pytest-cov`, `pydantic` 고정
3. **AC-FR-01-02 Red:** 빈칸 개수 검증 테스트·구현
4. **Track B:** `CompletionResolver.resolve()` 본 구현 (FR-02~05)
5. **README Track A/B TC 체크박스** Green 완료에 맞춰 갱신

---

## 11. 변경 이력

| 버전 | 일자 | 변경 내용 |
|------|------|-----------|
| 1.0 | 2026-05-29 | AC-FR-01-01 TDD Red-Green 세션 보고서 최초 작성 |
